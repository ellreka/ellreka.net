export const meta = {
  date: '2021-03-02',
  title: 'Chrome 拡張で特定のタブをアクティブにする方法',
  tags: ['ChromeExtension', 'Tips']
}

## 方法

少しハマったので。

アクティブにしたいタブの ID を指定して`update`するだけ。

```tsx
chrome.tabs.update(id, {
  active: true
})
```

## 公式ドキュメント

https://developer.chrome.com/docs/extensions/reference/tabs/#method-update
